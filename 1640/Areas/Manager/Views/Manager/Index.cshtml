@model List<Faculity>
<div class=row-pt-2 pb-4>
    <div class="col-6">
        <h2 style="color:red">List of Faculity</h2>
    </div>
    <div class="col-6">
        <a asp-controller="Manager" asp-action="Create" class="btn btn-primary">Create a new Faculity</a>
    </div>
    <div>
        <table class="table table-bordered">
            <thread>
                <tr>

                    <td>Name</td>
                    <td>Description</td>
                    <td>Action</td>
                </tr>
            </thread>
            <tbody>
                @foreach (var faculity in Model)
                {
                    <tr>
                        <td>@faculity.Name</td>
                        <td>@faculity.Description</td>
                        <td>

                            <a asp-controller="Manager" asp-action="Edit" asp-route-id="@faculity.Id" class="btn btn-primary">Edit</a>
                            <a asp-controller="Manager" asp-action="Delete" asp-route-id="@faculity.Id" class="btn btn-danger">Delete</a>

                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}